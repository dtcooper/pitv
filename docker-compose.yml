services:
  player:
    image: ghcr.io/dtcooper/pitv-player:latest
    build:
      context: player
      args:
      - "DEBUG=${DEBUG:-0}"
    volumes:
      - "./player:/app"
    # devices:
    #   - /dev/vchiq:/dev/vchiq
    #   - /dev/snd:/dev/snd
